// @ts-ignore
// import { Storage } from '@monitor/collector-browser'
import BrowserCollector from '@monitor/browser';

const browser = new BrowserCollector.Collector('token', {})

// const aaa = new Storage({})
// console.log(aaa)

// aaa.updateConfig({ token: 'lightYear' })
// aaa.register({ a: 2 })
